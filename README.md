# [Prog. Système] C++ : Processus et signaux

- Exemple d'utilisation de la fonction `signal()`, et des signaux `SIGUSR1` et `SIGUSR2`
- Exemple d'utilisation de la fonction `alarm()`, et des signaux `SIGINT` et `SIGALRM`
